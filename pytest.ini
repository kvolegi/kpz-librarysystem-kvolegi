[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
python_classes = Test*
addopts = -v --tb=short --strict-markers --cov-config=.coveragerc
markers =
    unit: Unit tests
    integration: Integration tests
    tdd: TDD demonstration tests
